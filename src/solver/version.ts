/**
 * Public solver/version stamp used for:
 *
 * - request/result transparency in the UI
 * - cache invalidation for persisted workbench state
 *
 * Bump this whenever request/result semantics or solver behavior change in a
 * way that should invalidate persisted browser state.
 */
export const SOLVER_VERSION = '2026.03.20.1';
